.venv/
venv/
__pycache__/
*.pyc
*.log
*.sqlite3
*.db
config/config.json
.env

# Voice runtime data: the enrolled owner's biometric embedding (data/voice_profile.json)
# and the downloaded SpeechBrain ECAPA model (data/models/), plus anything else the app
# writes to data/ at runtime. None of this belongs in version control.
data/