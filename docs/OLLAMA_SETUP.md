# Ollama Setup

Ollama is optional for JARVIS 2.0. The assistant continues to work offline in deterministic mode.

## Notes

- No model is installed or pulled automatically.
- The provider integration uses the local Ollama HTTP API at a localhost URL.
- Configure the model in config/config.json or config/settings.py.
- Use the command ai status to check whether AI is available.
